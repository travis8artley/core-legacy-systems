# Contributing

## Development Loop
1. Implement in small scoped changes.
2. Add or update tests.
3. Run `make test` locally.
4. Open a PR with context and tradeoffs.

## Commit Style
- `feat:` behavior additions
- `fix:` behavior corrections
- `docs:` narrative/design updates
- `ci:` pipeline changes
